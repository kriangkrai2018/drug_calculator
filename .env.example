# Example environment file for development — DO NOT commit real secrets here
# You can either set individual MYSQL_* vars OR use DATABASE_URL

# Using individual variables:
MYSQL_HOST=gateway01.ap-southeast-1.prod.aws.tidbcloud.com
MYSQL_PORT=4000
MYSQL_USER=your_db_user
MYSQL_PASSWORD=your_db_password
MYSQL_DATABASE=drug_calculator_db
MYSQL_CHARSET=utf8mb4
# Set to 'true' if your DB requires TLS/SSL (TiDB often does)
MYSQL_SSL=true
# If your TLS setup requires skipping verification (not recommended for production):
# MYSQL_SSL_REJECT_UNAUTHORIZED=false

# Or use a single connection URL (preferred in some deployments):
# DATABASE_URL=mysql://user:password@gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/drug_calculator_db?charset=utf8mb4&ssl=true&rejectUnauthorized=false

# Optional: connection pool size
# MYSQL_CONN_LIMIT=10
